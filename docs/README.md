# Documentation

Documentation has moved to `apps/docs/` and is built with [Astro Starlight](https://starlight.astro.build/).

```bash
pnpm dev:docs    # dev server at localhost:4321
pnpm docs:build  # static build to apps/docs/dist
```

Published at https://thomasdavis.github.io/donto/
